import { HidenDirective } from './hide.directive';

describe('HidenDirective', () => {
  it('should create an instance', () => {
    const directive = new HidenDirective();
    expect(directive).toBeTruthy();
  });
});
